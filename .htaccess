<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect all requests to public folder
    # This handles the subdirectory structure on Hostinger
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

